name: Deploy

# main 에 머지되면: 테스트 → 이미지 빌드/푸시(GHCR) → EC2 에서 pull & 재기동.
on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE: ghcr.io/${{ github.repository_owner }}/readforest-backend
  NGINX_IMAGE: ghcr.io/${{ github.repository_owner }}/readforest-nginx

# 기본은 권한 없음 — 각 job 에서 필요한 최소 권한만 부여
permissions: {}

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - name: Backend tests
        working-directory: readforest
        run: |
          chmod +x gradlew
          ./gradlew test --no-daemon
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Frontend build
        working-directory: frontend
        run: |
          npm ci
          npm run build

  build-and-push:
    name: Build & push images
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push backend
        uses: docker/build-push-action@v6
        with:
          context: ./readforest
          file: ./readforest/Dockerfile
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:latest
            ${{ env.BACKEND_IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push nginx (frontend bundled)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./nginx/Dockerfile
          push: true
          tags: |
            ${{ env.NGINX_IMAGE }}:latest
            ${{ env.NGINX_IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to EC2
    needs: build-and-push
    runs-on: ubuntu-latest
    permissions:
      packages: read   # 서버가 GHCR 에서 pull 할 때 쓰는 GITHUB_TOKEN
    steps:
      - name: SSH & roll out
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: ${{ secrets.EC2_SSH_PORT || 22 }}
          envs: GHCR_USER,GHCR_TOKEN,APP_DIR
          script: |
            set -e
            APP_DIR="${APP_DIR:-$HOME/Github/Read-Forest}"
            if [ ! -d "$APP_DIR/.git" ]; then
              echo "리포를 찾을 수 없습니다: $APP_DIR (접속 사용자: $(whoami), HOME: $HOME)"
              echo "EC2_USER 시크릿이 리포를 clone 한 사용자와 같은지,"
              echo "또는 EC2_APP_DIR 시크릿에 정확한 절대경로를 넣었는지 확인하세요."
              exit 1
            fi
            cd "$APP_DIR"
            # 추적 파일을 origin/main 에 강제 일치(.env·certbot/ 는 gitignore 라 보존)
            git fetch origin main
            git reset --hard origin/main
            echo "$GHCR_TOKEN" | docker login ghcr.io -u "$GHCR_USER" --password-stdin
            docker compose -f docker-compose.yml -f docker-compose.prod.yml pull
            docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --no-build
            docker image prune -f
            docker logout ghcr.io
        env:
          GHCR_USER: ${{ github.actor }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APP_DIR: ${{ secrets.EC2_APP_DIR }}
